# 结果解读（dual_patronage）

## 本次运行概览

- 观测轮次：`round = [0, 5, 10, 15, 20, 25, 30]`
- 生成轮次：`round = [0, 10, 20, 30]`
- 总生成数：`12`（每轮 3 张）

## 一句话结论

这次 `dual_patronage` 结果显示：网络结构从高模块化向更融合演化，同时头部可见性集中增强；生成端在不同轮次持续产生 `conform` 与 `diff` 混合样本，且后期样本的风格偏离中心略有上升。

## 核心指标变化（首末轮）

- `modularity`: `0.7261 -> 0.4790`（下降 `-0.2471`）
  - 含义：社群边界弱化，整体更趋向跨群连接。
- `cross_cluster_citation_rate`: `0.4500 -> 0.4328`（轻微下降 `-0.0172`）
  - 含义：跨簇引用总体保持在较高区间，但末轮略收缩。
- `in_degree_top10_share`: `0.0769 -> 0.2306`（上升 `+0.1537`）
  - 含义：注意力/引用向头部节点集中。
- `gini_in_degree`: `0.9281 -> 0.8546`（下降 `-0.0734`）
  - 含义：整体不平等仍高，但相比初期极端状态略缓和。

## 博弈层信号（game_rounds）

- `share_conform_topK`: `0.2625 -> 0.3500`（上升）
- `share_diff_topK`: `0.7375 -> 0.6500`（下降）
- `payoff_conform_cum_mean`: `0.125 -> 5.075`
- `payoff_diff_cum_mean`: `0.200 -> 5.900`

解释：两类策略都在累积收益，但 `diff` 累积收益仍更高；与此同时 TopK 中 `conform` 占比回升，说明系统并非单向“全面创新”，而是规范与差异并存的拉扯态。

## 生成层信号（generation_rounds）

- 生成统计均值：
  - `center_deviation_proxy` 均值约 `0.1757`
  - `rgb_std_mean` 均值约 `43.23`
- 首末轮：
  - `center_deviation_proxy`: `0.1917 -> 0.2130`（末轮略偏离中心）
  - `rgb_std_mean`: `55.68 -> 35.07`（末轮画面波动代理下降）

解释：末轮样本在风格空间中更偏离中心（与部分 `diff` 样本一致），但视觉复杂度代理并非单调上升，说明“创新程度”与“像素波动”不是同一维度。

## 如何把这些结果写进报告

可直接使用如下叙述模板：

1. 结构演化：`dual_patronage` 下模块化下降，表明社群边界减弱。  
2. 资源分配：头部引用份额上升，出现更明显的可见性集中。  
3. 策略博弈：`conform` 与 `diff` 共存，`diff` 累积收益更高但 `conform` 在 TopK 中回升。  
4. 生成映射：生成端在早中晚轮均输出两类策略样本，末轮风格偏离中心略增强。  

## 现阶段边界（诚实说明）

- 当前是“仿真先跑完，再生成”的映射版本，生成结果主要用于观测与解释，不直接反向驱动本轮演化。
- 若要升级为“真正风格演化”，应启用闭环：新图嵌入并作为新节点参与后续传播（已在代码中提供闭环开关）。
